type Animal {
    id: ID!
    name: String!
}

type Query {
    allAnimals: [Animal]!
    animalById(id: ID!): Animal!
}

type Mutation {
    newAnimal(name: String!) : Animal!
    deleteAnimal(id: ID!) : Boolean
}